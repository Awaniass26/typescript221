// Crée une fonction qui prend deux nombres en paramètres et retourne leur somme. 
// Définis les types des paramètres et de la valeur de retour.

function parametre(x:number , y:number):number{
    return x+y;
    
}
const resultat= parametre(2 , 3);
console.log(`la somme est:"${resultat}`);