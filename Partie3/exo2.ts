// Crée une fonction qui prend deux nombres en paramètre et retourne leur somme.
// Assure-toi que la fonction est correctement typée.
function para(a:number , b:number){
    return a+b
}
const result=para(4,12);
console.log(`le resultat est:${result}`)